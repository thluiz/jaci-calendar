# The source is edited on Windows and runs on Linux. LF everywhere: a systemd
# unit or a shell heredoc that picks up CRLF fails with a cryptic error.
* text=auto eol=lf
*.json text eol=lf
*.service text eol=lf
*.conf text eol=lf
